
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: s3bucketviews.ramendr.openshift.io
spec:
  group: ramendr.openshift.io
  names:
    kind: S3BucketView
    listKind: S3BucketViewList
    plural: s3bucketviews
    singular: s3bucketview
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: S3BucketView is the Schema for the s3bucketviews API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: S3BucketViewSpec defines the desired state of S3BucketView
            properties:
              profileName:
                description: ProfileName is the name of the S3 profile in the Ramen
                  operator config map specifying the bucket to be queried
                type: string
            required:
            - profileName
            type: object
          status:
            description: S3BucketViewStatus defines the observed state of S3BucketView
            properties:
              sampleTime:
                description: SampleTime is a timestamp representing the node time
                  when the specified S3 bucket was last queried. It is represented
                  in RFC3339 form and is in UTC.
                format: date-time
                type: string
              volumeReplicationGroups:
                description: VolumeRelicationGroups is a list of VolumeReplicationGroup
                  objects represented in the specified S3 bucket when it was last
                  queried.
                items:
                  properties:
                    annotations:
                      additionalProperties:
                        type: string
                      type: object
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
